import { AnimationType } from "./Animation";

export class EndWidgetData {
    public animation: AnimationType;

    constructor(_animation: AnimationType) {
        this.animation = _animation;
    }
}
